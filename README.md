# gha-view-enhancer

A Chrome extension (Manifest V3) that adds a filter toolbar to GitHub Actions
run pages, letting you hide matrix jobs by status (Success, Failure, Skipped,
Cancelled, In progress) so large matrices are easier to read.

Status: initial scaffold — see the open pull request for the first implementation.
